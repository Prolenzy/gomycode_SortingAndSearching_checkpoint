PROCEDURE insertion_sort(arr: ARRAY_OF INTEGER)
VAR
    i, n, j, key : INTEGER
BEGIN
    n := arr.LENGTH;
    
    FOR i FROM 1 TO n-1:
        key := arr[i]
        j := i - 1

    WHILE j >= 0 AND arr[j] > key:
        arr[j + 1] := arr[j]
        j := j - 1

    arr[j + 1] := key
END